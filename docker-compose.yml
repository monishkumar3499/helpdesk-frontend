version: "3.8"

services:
  frontend:
    container_name: helpdesk-frontend
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env